<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a3ti.school">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfessorPrincipalActivity" />
        <activity android:name=".EnviaSenhaActivity" />
        <activity android:name=".ErroLoginActivity" />
        <activity android:name=".PaisPrincipalActivity" />
        <activity android:name=".ChamadaActivity" />
        <activity android:name=".RElAlunosActivity" />
        <activity android:name=".RElFilhoActivity" />
        <activity android:name=".PaiEnviaMensagemActivity" />
        <activity android:name=".IncluirNotasActivity" />
        <activity android:name=".EventosActivity"></activity>
    </application>

</manifest>